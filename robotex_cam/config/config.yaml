#Parameters for the camera node

# Optical flow
of:
  refresh_interval: 8  # frames


frame:
  mask:
    saturation_thr:  80

contour:
  rect:
    min_size:      200000
    max_size:      6000
    min_ratio:      2.5
    max_ratio:      5.0
    max_angle:     55.0  # in degrees not radians
